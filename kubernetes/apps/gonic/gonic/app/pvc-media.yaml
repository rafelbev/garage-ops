---
# Dynamic PVC for Gonic media (NFS for shared access)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gonic-media
  namespace: gonic
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: truenas-nfs
  resources:
    requests:
      storage: 10Gi
